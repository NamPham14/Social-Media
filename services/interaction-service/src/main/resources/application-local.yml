spring:
  datasource:
    url: jdbc:postgresql://localhost:5436/interaction_service
    username: postgres
    password: 123456
  jpa:
    show-sql: true

internal:
  service-token: ${INTERNAL_SERVICE_TOKEN:local-development-token}
