meta {
  name: Follow User
  type: http
  seq: 2
}

post {
  url: http://localhost:8888/api/v1/follow
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}

body:json {
  {
    "followingId": "{{followingId}}"
  }
}
