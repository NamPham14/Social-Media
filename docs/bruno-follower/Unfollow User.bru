meta {
  name: Unfollow User
  type: http
  seq: 3
}

post {
  url: http://localhost:8888/api/v1/follow/unfollow
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}

body:json {
  {
    "followingId": "{{followingId}}"
  }
}
