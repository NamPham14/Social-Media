meta {
  name: Unfollow User
  type: http
  seq: 2
}

post {
  url: http://localhost:8084/api/v1/follow/internal/unfollow
  body: json
  auth: none
}

headers {
  X-Auth-User-Id: {{followerId}}
}

body:json {
  {
    "followingId": "{{followingId}}"
  }
}
