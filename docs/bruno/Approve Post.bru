meta {
  name: Approve Post
  type: http
  seq: 8
}

put {
  url: http://localhost:8082/api/v1/posts/approve
  body: json
  auth: none
}

body:json {
  {
    "postId": "{{postId}}",
    "adminId": "{{adminId}}"
  }
}
